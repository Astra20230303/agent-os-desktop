# AgentOS Desktop

这是 AgentOS 的桌面应用壳，使用 Electron 加载现有的全屏 macOS 风格工作区。

## 运行

```bash
npm install
npm start
```

窗口默认使用 macOS 隐藏标题栏、原生红黄绿窗口控制、半透明背景和 1024×640 最小尺寸。使用 `⌘⇧F`（Windows/Linux 为 `Ctrl⇧F`）切换原生全屏。
